import { ForbiddenAuthorDirective } from './forbidden-author.directive';

describe('ForbiddenAuthorDirective', () => {
  it('should create an instance', () => {
    const directive = new ForbiddenAuthorDirective();
    expect(directive).toBeTruthy();
  });
});
